interface User {
	username: string
	email: string
	phoneNumber: string
}

enum Gender {
	MALE = 'MALE',
	FEMALE = 'FEMALE',
	OTHER = 'OTHER'
}

interface Profile {
	firstName: string
	lastName: string
}
